/**
 * Referrer holds the details about the marketing referrer from which a client
 * is executing the event from.
 */
export interface Referrer {
  type?: string
  name?: string
  url?: string
  link?: string
}
